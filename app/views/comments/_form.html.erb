<%= form_for([@beat, @beat.comments.build], remote: true) do |f| %>
  <%= f.hidden_field :user_id, value: current_user.id %>

  <%= f.text_area :body, placeholder: "Write a comment..." %>

  <%= f.submit "Submit Comment", class: "button secondary radius" %>

<% end %>